{
  "name": "profitos-audit-engine",
  "version": "1.0.0",
  "private": true,
  "description": "WF-08 — ProfitOS Engine core computation engine. Receives validated audit payloads from WF-06, enriches in parallel (Apollo.io, Lusha, Semrush, Clay), scores profit leaks, and delivers a dollar-figure PDF report within 5 minutes.",
  "type": "module",
  "main": "src/server.js",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test 'test/*.test.js'",
    "smoke": "node test/smoke.js"
  },
  "dependencies": {
    "axios": "^1.7.7",
    "dotenv": "^16.4.5",
    "express": "^4.21.1",
    "googleapis": "^144.0.0",
    "helmet": "^8.0.0",
    "nodemailer": "^6.9.15",
    "pdfkit": "^0.15.1",
    "puppeteer": "^23.6.0"
  },
  "license": "UNLICENSED"
}
